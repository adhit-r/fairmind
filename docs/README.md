# Engineering Docs

This root `docs/` directory now contains engineering and operational documentation for maintainers:

- `docs/architecture/` - system maps, boundaries, and reorg ledger
- `docs/operations/` - contributor/runbook guidance
- `docs/adr/` - architecture decision records

Product/user-facing documentation is hosted from the dedicated Fumadocs app at:

- `apps/docs` (deployment target: `docs.fairmind.xyz`)

Marketing site remains:

- `apps/website` (deployment target: `fairmind.xyz`)
